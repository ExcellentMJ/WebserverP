<nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">HOME</a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/books"
            >도서검색</a
          >
        </li>
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/locals"
            >지역검색</a
          >
        </li>
        <li class="nav-item" id="cart" style="display: none">
          <a class="nav-link active" aria-current="page" href="/books/cart"
            >장바구니</a
          >
        </li>
      </ul>
      <ul class="navbar-nav d-flex">
        <!-- d-flex : 우측 정렬 -->
        <li class="nav-item" id="login">
          <a class="nav-link active" aria-current="page" href="/users/login"
            >LOGIN</a
          >
        </li>
        <li class="nav-item" id="email" style="display: none">
          <a class="nav-link active" aria-current="page" href="/users/mypage"
            >email</a
          >
        </li>
        <li class="nav-item" id="logout" style="display: none">
          <a class="nav-link active" aria-current="page" href="/users/logout"
            >LOGOUT</a
          >
        </li>
      </ul>
    </div>
  </div>
</nav>
<script>
  // 로그인 로직
  /*로그인 했을 때
    1. id가 login인거 Toggle로 보임->안보임, id가 email인거 Toggle로 안보임->보임, id가 logout인거 Toggle로 안보임->보임
    2. id가 email인거라벨에 세션의 email정보 불러와서 그 값을 넣어줌*/
  /*로그아웃 했을 때
    1. id가 login인거 Toggle로 보임->안보임, id가 email인거 Toggle로 안보임->보임, id가 logout인거 Toggle로 안보임->보임
    2. 세션 클리어링
    3. 다시 메인화면(/)으로 redirect*/
  if (sessionStorage.getItem("email")) {
    $("#login, #email, #logout, #cart").toggle();
    $("#email a").html(sessionStorage.getItem("email"));
  }
  $("#logout").on("click", "a", function (e) {
    e.preventDefault();
    if (confirm("로그아웃하시겠습니까? 진심?")) {
      sessionStorage.clear();
      location.href = "/";
    } else {
      return;
    }
  });
</script>
